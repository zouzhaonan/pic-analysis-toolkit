# PIC analysis toolkit

Project layout:
- `bin/` entrypoint (`pic`)
- `help/` runtime help text files consumed by CLI
- `scripts/` awk/bash/R implementations
- `src/recipe/` conda package recipe
- `docs/` user/developer markdown documents
